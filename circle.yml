dependencies:
   pre:
     - pip install -U pip
     - pip install tox

test:
  override:
    - which python
    - type python
    - whereis python
    - pip install tox
    - pwd
    - ls
    - tox -v --recreate

