machine:
  post:
    - pyenv local 3.4.3 3.3.3 3.2.5 3.1.5 2.7.10 2.6.8


dependencies:
   pre:
     - pip install -U pip
     - pip install -U tox

test:
  override:
    - pwd
    - ls
    - tox -v --recreate

